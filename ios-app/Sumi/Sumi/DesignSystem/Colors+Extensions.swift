import SwiftUI

extension Color {
    // Primary Colors
    static let sumiInk = Color(hex: "#1A1614")
    static let washi = Color(hex: "#F2EDE4")
    
    // Accent Colors
    static let matcha = Color(hex: "#6B8F71")
    static let persimmon = Color(hex: "#C65D2A")
    
    // Derived Colors
    static let washiDark = Color(hex: "#2A2420")
}
